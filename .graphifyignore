# Files deliberately kept out of the graphify knowledge graph.
#
# Without this file, `graphify update` re-ingests them and adds ~237 junk nodes
# (one per CHANGELOG version heading), which also re-shatters the communities.

# Autogenerated release history: describes the past, not the current design.
CHANGELOG.md

# Tool documentation, not product documentation. Pollutes the communities.
.claude/skills/

# Favicon: a vision pass over it costs tokens and yields nothing.
src/app/icon.svg
